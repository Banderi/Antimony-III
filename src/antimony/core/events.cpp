#include "events.h"
#include "SDL2/SDL_events.h"

void UpdateEvents() {
    // update mouse
    if (!SDL_GetRelativeMouseMode()) {

    }
    // update window events
    // update keyboard events
    // update other events
}