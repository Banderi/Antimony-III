#include "events.h"

#include <GID_SDL.h>
#include GID_SDL(SDL_events.h)


void UpdateEvents() {
    // update mouse
    if (!SDL_GetRelativeMouseMode()) {

    }
    // update window events
    // update keyboard events
    // update other events
}